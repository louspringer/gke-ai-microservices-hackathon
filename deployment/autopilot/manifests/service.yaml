apiVersion: v1
kind: Service
metadata:
  name: systematic-app-service
  namespace: hackathon-app
  labels:
    app: systematic-app
    beast-mode: "systematic-excellence"
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  selector:
    app: systematic-app